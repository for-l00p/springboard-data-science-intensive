This contains a Jupyter notebook employing logistic regression to explore  predicting gender from weights and heights. The notebook uses the packages pandas, matplotlib, scikit-learn, scipy, and seaborn.
